name: indexed_map
description: A high-performance hybrid data structure providing O(1) lookup by ID and ordered/indexed access, with comprehensive duplicate handling policies.
version: 1.1.0
homepage: https://github.com/NeuralMobile/indexed_map
repository: https://github.com/NeuralMobile/indexed_map
issue_tracker: https://github.com/NeuralMobile/indexed_map/issues

environment:
  sdk: ^3.8.1

dependencies:
  # No runtime dependencies

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0
  benchmark_harness: ^2.2.2
